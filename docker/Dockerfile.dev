FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

# Use direct node command - env vars are passed via docker-compose
CMD ["node", "--experimental-transform-types", "--watch", "src/index.ts"]
